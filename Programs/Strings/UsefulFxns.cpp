// To use awesome functions import two new header files algorithm and string 😎
#include<iostream>
#include<string>
#include<algorithm>

using namespace std;

int main()
{
    // to input a string
    //string str;
    //getline(cin,str);

    // declaring a string of 10 size with one character
    //string one(10,'X');

    /*

    ⚠ Useful Functions ⚠

    🔷append(): Inserts additional characters at the end of the string (can also be
    done using ‘+’ or ‘+=’ operator). Its time complexity is O(N) where N is the
    size of the new string
    FORMAT=> s1.append(s2)

    🔶assign(): Assigns new string by replacing the previous value (can also be
    done using ‘=’ operator).
    FORMAT => s1.assign("Natkhat America")

    🔷at(): Returns the character at a particular position (can also be done using ‘[i]'
    operator). Its time complexity is O(1).
    FORMAT => s.at(3)

    🔶s.begin(), tells us the starting index of the string s and s.end() tells us the ending index of the string s.

    🔷s.clear(), removes the value given in the string and makes it an empty string

    🔶s1.compare(S2), Compares the value of the string given as the parameter with the string upon which the function is called, if the output is 0, it means the strings are equal, if positive output then the other string is greater else the first string is greater.

    🔷s.empty(), returns true if the string s is empty or else return false

    🔶s.erase(strt_indx, No_chars) => erases the given characters in the string including the starting index 

    🌟s.find("demo"), searches the string for the argument and returns the index of the first word of the argument if found!

    🌟s.insert(index,"What to insert"), inserts the given argument at the index provided.

    🌟s.length() or s.size(), both of them returns the size of the given string!

    🌟s.resize(size) , resizes the string to the given size which can be less than or greater than the current size.

    🌟s.substr(index, No_Chars) , returns a substring from the index to the no of characters ahead of index.

    🌟stoi(s) , converts the String TO Integer, and to convert an integer to string, we use the to_string(n), function

    ⭐ Sorting a string (⚠included in the algorithm header file!)
    FORMAT,
    sort(s.begin(),s.end())

    */


   cout<<"Have a nice revision!"<<endl;
   cout<<"Kamui!"<<endl;
    return 0;
}